# Person detector model

Run `python tools/install_yolo_model.py` to download the pinned `yolov8n.onnx`
file and verify its SHA-256 checksum. The binary is intentionally excluded from
Git. Review the upstream model and license terms before commercial deployment.
